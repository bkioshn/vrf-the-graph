enum Status {
  PENDING
  REJECTED
  APPROVED
}

type DataRequest @entity {
  id: ID!
  sender: Bytes
  seed: String
  time: BigInt
  bounty: BigInt
  status: Status
  block: BigInt
  # param: String
}

type RelayProof @entity{
  id: ID!
  seed: String
  time: BigInt
  status: Status
}